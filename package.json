{
  "name": "relationship-variable-reward-scheduler",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.5.2",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "local:start": "bash tools/local/run-local.sh",
    "local:start:no-worker": "bash tools/local/run-local.sh --no-worker",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "format": "prettier --check .",
    "security:waivers": "node tools/security/check-waivers.mjs"
  },
  "devDependencies": {
    "@types/node": "25.2.2",
    "eslint": "10.0.0",
    "js-yaml": "4.1.1",
    "prettier": "3.8.1",
    "typescript": "5.9.3"
  }
}
